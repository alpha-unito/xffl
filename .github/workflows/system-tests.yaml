name: "System Tests"

on:
  push:
    branches:
      - main

jobs:
  cli-config:
    name: "Test xffl config"
    strategy:
      matrix:
        on: [ "ubuntu-24.04"]
        python: [ "3.10", "3.11", "3.12" ]
    runs-on: ${{ matrix.on }}
    steps:

      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: 3.11
          cache: pip
          cache-dependency-path: requirements.txt

      - name: "Install Python Dependencies"
        run: python -m pip install .

      - name: "Create an xffl project using the config CLI interface."
        env:
          CHECKSUM: "2962309eecc0505d112d1eeb4bbdbe4d06e91f136f72a187a520dd68a5c11f03"
        run: |
          cd test/system_test/cli
          HASH="$(make checksum-config | tail -n 1)"
          echo "Project checksum is ${HASH}"
          test "${HASH}" == "${CHECKSUM}"

      - name: "Validate project"
        run: |
          cd test/system_test/cli
          make setup
          make generate-project
          xffl run --project test --validate
          make clean
